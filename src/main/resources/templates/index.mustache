<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>파일 확장자 차단</title>
    <link rel="stylesheet" href="css/styles.css">
    <script src="https://code.jquery.com/jquery-3.4.1.js"
            integrity="sha256-WpOohJOqMqqyKL9FccASB9O0KwACQJpFTUBLTYOVvVU="
            crossorigin="anonymous"></script>
    <script src="https://kit.fontawesome.com/f6f4811ccf.js" crossorigin="anonymous"></script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
</head>
<body>
<h1>파일 확장자 차단</h1>
<hr>
<p class="file-restriction-description">파일 확장자에 따라 특정 형식의 파일을 첨부하거나 전송하지 못하도록 제한</p>
<div class="table-container">
<table class="extension-table">
    <tr>
        <th>고정 확장자</th>
        {{#fixedExtensionList}}
            <td class="extension-cell">
                <input type="checkbox" class="update-fixed-extension" id={{id}} value="{{use}}" name={{name}}>
                <span> {{name}}</span>
            </td>
        {{/fixedExtensionList}}
    </tr>
    <tr>
        <th>커스텀 확장자</th>
        <td colspan="4">
            <input type="text" id="custom-extension-name" class="custom-extension-input" placeholder="확장자 입력"/>
        </td>
        <td>
            <button type="button" id="insert-custom-extension" class="custom-extension-btn">
                <i class="fa-solid fa-plus"></i>
                추가
            </button>
        </td>
    </tr>
    <tr>
        <th></th>
        <td colspan="7" class="custom-extension-list">
            <div class="custom-extension-container">
                <p class="custom-extension-size">{{customExtensionListSize}}/{{customExtensionMaxSize}}</p>
                <div class="custom-extension-items">
                    {{#customExtensionList}}
                        <span class="custom-extension-item">
                    {{name}}
                            <button id="{{id}}" name="{{name}}" type="button" class="delete-custom-extension">
                        <i class="fa-solid fa-xmark"></i>
                    </button>
                </span>
                    {{/customExtensionList}}
                </div>
            </div>
        </td>

    </tr>
</table>
</div>
<script src="js/insertCustomExtension.js"></script>
<script src="js/deleteCustomExtension.js"></script>
<script src="js/updateFixedExtension.js"></script>
<script>
    const checkboxes = document.querySelectorAll('input[type=checkbox].update-fixed-extension[value="true"]');
    checkboxes.forEach((checkbox) => {
        checkbox.checked = true;
    });
    </script>
</body>
</html>